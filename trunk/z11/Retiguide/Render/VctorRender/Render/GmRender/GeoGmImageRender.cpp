#include "GeoGmImageRender.h"

namespace GIS_Embedded
{
	int GeoGmImageRender::render(StyleRender& mg)
	{
 		//GeometryImage* gm_image = (GeometryImage*)m_geometry;
 
 		//long left,top,right,bottom;
 		//if(m_transformer->transfromMapToScr(gm_image->_bound.m_left,gm_image->_bound.m_top,&left,&top) >=0
 		//		&& m_transformer->transfromMapToScr(gm_image->_bound.m_right,gm_image->_bound.m_bottom,&right,&bottom) >=0)
 		//	mg.drawImage(mImageHandle,left,top,right,bottom);

		return 0;
	}

}
